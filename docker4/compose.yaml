services:
  profile:
    image: redis
    volumes:
      - me:/data

  avatar:
    image: ghcr.io/infratify/arena-avatar
    ports:
      - "8080:8080"
    environment:
      NAME: arifin
      COLOR: orange
      SERVER: 13.213.70.70
    depends_on:
      - profile

volumes:
  me:
